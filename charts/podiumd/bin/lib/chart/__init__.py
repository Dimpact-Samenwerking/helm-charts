"""Chart.yaml/values.yaml resolution helpers, split out of the former
lib/chart.py (deleted once its own pylint too-many-lines split finished
— see values_tree_primitives, registered_paths, nested_subchart_identity,
release_baseline_basics, pull_and_subchart_resolution,
repo_and_path_resolution, historical_baselines). No shared content of
its own — every name lives in one of these siblings."""
