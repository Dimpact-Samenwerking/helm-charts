"""Helpers for the fix-doc-consistency script's own pylint too-many-lines
split (see text_helpers_and_repo_rename, baseline_and_images_manifest_
handling, manifest_changes_items, manifest_entries_core, manifest_entries_
new_and_urls, component_version_table). fix-doc-consistency itself
(a hyphenated, non-importable script) is the only consumer — its own
main() and CLI-adjacent constants stay in the script, never here."""
