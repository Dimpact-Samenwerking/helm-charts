"""export-confluence-release-table's own component/basename resolution
helpers, split out of that script for pylint's too-many-lines check —
see component_resolution (name/alias -> Chart.yaml dependency) and
image_basenames (resolved component -> values.yaml image basename).
No shared content of its own — every name lives in one of these
siblings."""
