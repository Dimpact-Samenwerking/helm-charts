"""Upgrade-doc (docs/_UPGRADE_PATHS/*.md) parsing/formatting helpers,
split out of the former lib/upgradedoc.py (deleted once its own pylint
too-many-lines split finished — see string_and_parsing_basics,
sorting_and_ordering, consistency_checks, resolve_component_row,
version_cells_and_key_changes, app_version_and_image_paths,
images_manifest_ordering, images_manifest_list_diff,
grouped_comments_and_changes_block). No shared content of its own —
every name lives in one of these siblings."""
