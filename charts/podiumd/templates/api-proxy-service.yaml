apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-proxy.fullname" . }}
spec:
  selector:
    app: {{ include "api-proxy.name" . }}
  ports:
  - port: {{ .Values.apiproxy.service.port }}
    targetPort: http
    name: http
  type: ClusterIP
