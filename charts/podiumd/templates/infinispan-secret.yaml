{{- if .Values.infinispan.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-generated-secret" (include "infinispan-helm-charts.name" .) }}
  labels:
    app: infinispan-secret-identities
  {{- include "infinispan-helm-charts.labels" . | nindent 4 }}
  annotations:
  {{- include "infinispan-helm-charts.annotations" . | nindent 4 }}
    "helm.sh/resource-policy": keep
type: Opaque
stringData:
  username: monitor
  password: {{ .Values.infinispan.security.monitor.secret }}
  {{- if .Values.infinispan.deploy.security.batch }}
  identities-batch: |-
    {{- .Values.infinispan.deploy.security.batch | trim | nindent 4 }}
  {{- end }}
{{- end }}