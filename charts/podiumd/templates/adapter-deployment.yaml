{{- if .Values.kiss.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "podiumd.fullname" . }}-adapter
  labels:
    {{- include "podiumd.labelsAdapter" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "podiumd.selectorLabelsAdapter" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/adapter-config.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/adapter-secret.yaml") . | sha256sum }}
      labels:
        {{- include "podiumd.labelsAdapter" . | nindent 8 }}
    spec:
      {{- with .Values.kiss.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "podiumd.serviceAccountName" . }}
      containers:
        - name: adapter
          image: {{ include "podiumd.image" .Values.kiss.adapter.image }}
          imagePullPolicy: {{ .Values.kiss.adapter.image.pullPolicy }}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            {{- toYaml .Values.kiss.adapter.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: adapter-config
            - secretRef:
                name: adapter-secret
          {{- if .Values.kiss.adapter.extraEnvVars }}
          env:
            {{- toYaml .Values.kiss.adapter.extraEnvVars | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          {{- if .Values.kiss.adapter.extraVolumeMounts }}
            {{- include "kiss-frontend.tplvalues.render" ( dict "value" .Values.kiss.adapter.extraVolumeMounts "context" $ ) | nindent 12 }}
          {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
      {{- if .Values.kiss.adapter.extraVolumes }}
        {{- include "kiss-frontend.tplvalues.render" ( dict "value" .Values.kiss.adapter.extraVolumes "context" $ ) | nindent 8 }}
      {{- end }}
      {{- with .Values.kiss.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
