# Images new or changed in podiumd 4.8.0 vs 4.7.3.
#
# Single change: Open Inwoner Platform (OIP) 2.1.2 -> 2.3.0.
#   * Helm chart openinwoner 2.1.3 -> 2.2.0 (appVersion 2.3.0).
#   * Image tag pin in charts/podiumd/values.yaml: openinwoner.image.tag
#     "2.1.2" -> "2.3.0".
# Spans two upstream releases:
#   - 2.2.0 (2026-04-20): Django CMS v3 -> v4 (one-time `cms4_migration`,
#     run by the chart's cms4MigrationInitContainer), Elasticsearch HTTP
#     basic auth, BRP_VERSION env -> admin-configurable field (auto-migrated).
#   - 2.3.0 (2026-05-29): ClamAV virus scanning of uploads (opt-in),
#     ZGW cache-warmup timeouts + dedicated low-latency Celery worker.
# See docs/upgrade-from-4.7.3-to-4.8.0.md for the operator upgrade notes.
#
# Mirror name is `openinwoner` (no hyphen), not `open-inwoner`; see
# docs/images/acr-mirror-naming.md for the upstream->ACR mapping.
# Digest below is the single linux/amd64 manifest published on Docker Hub,
# verified live against registry-1.docker.io (Docker-Content-Digest).
# The nginx sidecar stays on nginx-unprivileged 1.30.2 (unchanged).

# Open Inwoner Platform (OIP) — 2.1.2 -> 2.3.1
#   2.3.1 (2026-06-11) is a bug-fix patch over 2.3.0 (no new config, no breaking
#   changes); chart openinwoner 2.2.0 -> 2.2.1 (appVersion 2.3.1).
#   Applied on the feature/podiumd-4.8.0-ontw-mayk-test-updates branch.
- name: openinwoner
  url: docker.io/maykinmedia/open-inwoner
  version: "2.3.1"
  digest: "sha256:3a2ff5c8eefa69252b15a7385c82811c3681e6ecc29bb2aa35eccb331dddbc56"

# KISS frontend — 2.2.2 -> 2.2.3

- name: contact-frontend
  url: ghcr.io/klantinteractie-servicesysteem/kiss-frontend
  version: "2.2.3"
  digest: "sha256:a12276517ce7846e5ebe40373de8c4960fb41182958a668ad60254b088252252"
- name: contact-sync
  url: ghcr.io/klantinteractie-servicesysteem/kiss-elastic-sync
  version: "0.3.3"
  digest: "sha256:f26bd91d96ec8f1875e1ce7a2386788913240bc3b70a520926db444ba61e424c"

# ITA — 3.1.0 -> 3.2.0
- name: internetaakafhandeling.web
  url: ghcr.io/interne-taak-afhandeling/internetaakafhandeling.web
  version: "3.2.0"
  digest: "sha256:159489884a041ec9222e6e2a0f0c5ff2a70f93d7b7885fa19c6be2c59d658015"
- name: internetaakafhandeling.poller
  url: ghcr.io/interne-taak-afhandeling/internetaakafhandeling.poller
  version: "3.2.0"
  digest: "sha256:eee4567fd5bfaa7eb2ec13fa1a34cfabb402939ffdc79de8159f5dc00754bafb"

# ClamAV daemon — 1.4.4 -> 1.5.2 (image-tag override; wiremind clamav chart
#   stays 3.7.1, already the latest). 1.5.2 fixes CVE-2026-20031 (HTML-parser
#   DoS) plus crash fixes; ClamAV 1.5 is a feature release with no breaking
#   clamd.conf changes. Applied on feature/podiumd-4.8.0-ontw-mayk-test-updates.
#   clamav/clamav is multi-arch; the digest below is the OCI image-index
#   (manifest-list) digest, which is registry-agnostic (amd64 layer is
#   sha256:a2093dd28ea7b4b6639298f33181d9ee3044375d75fbe2a2e2a2667a4c820a92).
- name: clamav
  url: docker.io/clamav/clamav
  version: "1.5.2"
  digest: "sha256:c17762a00ba24dce4cf822fdc6f5bdfaa6e3ba152efc734d545b9a68be5641da"
