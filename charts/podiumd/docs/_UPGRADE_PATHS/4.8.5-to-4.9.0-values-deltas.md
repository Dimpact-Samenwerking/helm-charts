# Values deltas — PodiumD 4.8.5 → 4.9.0

**No gemeente `podiumd.yml` changes are required for this hop.**

All changes ship entirely in the chart `values.yaml`/`Chart.yaml` defaults:

- **ZAC** app `5.0.2 → 5.1.0` (chart `1.0.297`, unchanged) — image tag only.
- **ZGW Office Add-in** frontend + backend `v0.9.313 → v0.9.352` (chart
  `0.0.89`, unchanged) — image tags only.
- **ClamAV** `1.5.2 → 1.5.4` (chart `3.7.1 → 3.7.2`) — image tag + chart
  version only.
- **Kiss's ECK-managed Elasticsearch/Kibana/Enterprise Search**
  `8.19.3 → 8.19.19` — CR `version:` fields only.
- **ECK Operator** `3.4.0 → 3.5.0` (chart + image) and **ECK Stack (kiss-eck)**
  `0.19.0 → 0.20.0` (chart only) — see
  [operators & CRDs](4.8.5-to-4.9.0-operators-crds.md) for the CRD impact.
- **Keycloak** app image `26.6.4 → 26.7.2` — operator chart unchanged
  (`1.12.1`).
- **Python** (ensurePodiumdAdminUser init image) `3.14-slim → 3.14.7-slim` —
  now pinned to a specific patch instead of the floating minor tag.

Every environment checked overrides `registry`/`repository` for these images
(their own ACR mirror) but never `tag`, so the new tags/digests apply from the
chart defaults automatically.

The only prerequisite is mirroring the new digests — all listed in
[`images-4.9.0.yaml`](../images/images-4.9.0.yaml).
