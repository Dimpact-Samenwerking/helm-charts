# Operators & CRDs — PodiumD 4.5.15 → 4.5.16

Companion to [`4.5.15-to-4.5.16-upgrade.md`](4.5.15-to-4.5.16-upgrade.md).

| Component | Change | CRD impact | Action |
|---|---|---|---|
| keycloak-operator | Operator image `26.5.6` → `26.5.7` (server bumped in step) | None | ACR mirror the new tag; ACR environments set the repository overrides below |

No CRD additions or migrations in this hop.

```yaml
keycloak:
  image:
    repository: <acr>/keycloak

keycloak-operator:
  operator:
    image:
      repository: <acr>/keycloak-operator
```

Tags come from the chart defaults (`26.5.7`) — no tag overrides needed.
