# Values deltas — PodiumD 4.9.1 → 4.9.2

## keycloak-operator 26.7.3 (unchanged) (chart 1.13.0, unchanged)

- Key `keycloak-operator.jobs.ensurePodiumdAdminUser.image.pullPolicy` was added.
- Key `keycloak-operator.jobs.ensurePodiumdAdminUser.image.registry` was removed.

## Open Formulieren 3.5.6 → 3.5.8 (chart 1.12.0, unchanged)

- Key `openformulieren.redis` was removed.
- Key `openformulieren.customerInteractionsConfigJob` was added (opt-in Job that creates the Customer interactions API group for Open Klant — see `4.9.1-to-4.9.2-gemeente-specific.md`).

## referentielijsten 0.7.3 → 0.7.4 (chart 0.1.1 → 0.2.0)

- Key `referentielijsten.redis` was removed.

## eck-operator 3.5.0 (new) (chart 3.5.0, unchanged)

- Key `eck-operator.image` was added.

## openbao v2.5.5 (new) (chart 0.28.4, unchanged)

- Key `openbao.database.schemaJob.image.pullPolicy` was added.

## Keycloak client URLs (no component version change)

Defaults changed from an `example.nl` URL to `""`; the render now fails while
the component or client is enabled and the value is unset. See the
[upgrade guide](4.9.1-to-4.9.2-upgrade.md#keycloak-client-urls-no-more-examplenl-defaults).

- `keycloak.config.clients.monitoring.oidcUrl`, `keycloak.config.clients.datamigratie.oidcUrl`, `keycloak.config.clients.zaakbrug.oidcUrl`
- `openzaak`, `opennotificaties`, `objecten`, `objecttypen`, `openarchiefbeheer`, `openklant`, `openformulieren`, `openinwoner`, `referentielijsten`, `openbeheer`, `kiss`, `openbao`: `<component>.configuration.oidcUrl`
- `zac.contextUrl`
- `pabc.settings.oidc.oidcUrl`
- `ita.web.oidc.frontendUrl` (was `REP_ITA_OIDC_FRONTEND_URL_REP`)

## `nodeSelector` on every component (no component version change)

In [`values.yaml`](../../values.yaml) a number of components have
`nodeSelector: {}`, so the chart does not choose a node pool for them. Their
pods can then be scheduled on any node, the system pool included.

> **Required:** logs of a component are **only visible in Grafana** when its
> pods run on the `userpool` node pool. Set the `nodeSelector` below for
> **every** component in the list, or the logs of that component will be
> missing from Grafana.

```yaml
nodeSelector:
  kubernetes.azure.com/agentpool: userpool
```

This applies to every gemeente and every environment (`ontw`, `test`, `accp`
and `prod`). Set it in `<gemeente>/<env>/podiumd.yml` before deploying
PodiumD 4.9.2. Also set it for components that are disabled in an
environment, so it is correct when they are turned on later.

### Components with `nodeSelector: {}` in `values.yaml`

| Component | Key in `podiumd.yml` |
|---|---|
| Redis HA label-master CronJob | `redis-operator.redis-ha.labelMasterCronJob.nodeSelector` |
| Redis HA pre-delete Job | `redis-operator.redis-ha.preDeleteJob.nodeSelector` |
| KISS Elasticsearch | `kiss-eck.eck-elasticsearch.nodeSets[].podTemplate.spec.nodeSelector` |
| KISS Enterprise Search | `kiss-eck.eck-enterprise-search.podTemplate.spec.nodeSelector` |
| KISS Kibana | `kiss-eck.eck-kibana.podTemplate.spec.nodeSelector` |
| KISS | `kiss.nodeSelector` |
| ZAC Solr operator | `zac.solr-operator.nodeSelector` |
| API proxy | `apiproxy.nodeSelector` |
| ITA | `ita.nodeSelector` |
| PABC | `pabc.nodeSelector` |
| PABC migrations | `pabc.migrations.nodeSelector` |
| Frank gateway | `frankgateway.nodeSelector` |
| Frank gateway SSL sync | `frankgateway.tls.sslSync.nodeSelector` |
| Frank gateway etcd | `frankgateway.etcd.nodeSelector` |
| Frank gateway seed Job | `frankgateway.seed.job.nodeSelector` |
| Frank gateway client-cert sync | `frankgateway.clientCertSync.nodeSelector` |
| Frank gateway dashboard | `frankgateway.dashboard.nodeSelector` |
| Frank gateway dashboard oauth2-proxy | `frankgateway.dashboard.auth.oauth2Proxy.nodeSelector` |
| Frank gateway dashboard auth shim | `frankgateway.dashboard.auth.shim.nodeSelector` |
| OpenBao configuration Job | `openbao.configuration.job.nodeSelector` |
| OpenBao database schema Job | `openbao.database.schemaJob.nodeSelector` |

Keep a `nodeSelector` that a gemeente already sets in its `podiumd.yml`, as
long as it points to the `userpool`.

### Note on KISS Elasticsearch `nodeSets`

`kiss-eck.eck-elasticsearch.nodeSets` is a list. Helm replaces a list as a
whole, it does not merge it. So when you set the `nodeSelector` here, copy the
**complete** nodeSet from `values.yaml` (`name`, `count` and `config`), not
only `podTemplate`. Leaving out `count` or `config` changes the Elasticsearch
cluster. If the environment already overrides `nodeSets` (for example with
`volumeClaimTemplates`), add `podTemplate` to that nodeSet and keep the rest.

### Snippet for `<gemeente>/<env>/podiumd.yml`

Merge these keys into the existing sections of the file; do not add a second
top-level key for a component that is already there.

```yaml
redis-operator:
  redis-ha:
    labelMasterCronJob:
      nodeSelector:
        kubernetes.azure.com/agentpool: userpool
    preDeleteJob:
      nodeSelector:
        kubernetes.azure.com/agentpool: userpool

kiss-eck:
  eck-elasticsearch:
    nodeSets:
    - name: default
      count: 3
      config:
        node.roles:
        - data
        - ingest
        - master
        node.store.allow_mmap: false
      podTemplate:
        spec:
          nodeSelector:
            kubernetes.azure.com/agentpool: userpool
  eck-enterprise-search:
    podTemplate:
      spec:
        nodeSelector:
          kubernetes.azure.com/agentpool: userpool
  eck-kibana:
    podTemplate:
      spec:
        nodeSelector:
          kubernetes.azure.com/agentpool: userpool

kiss:
  nodeSelector:
    kubernetes.azure.com/agentpool: userpool

zac:
  solr-operator:
    nodeSelector:
      kubernetes.azure.com/agentpool: userpool

apiproxy:
  nodeSelector:
    kubernetes.azure.com/agentpool: userpool

ita:
  nodeSelector:
    kubernetes.azure.com/agentpool: userpool

pabc:
  nodeSelector:
    kubernetes.azure.com/agentpool: userpool
  migrations:
    nodeSelector:
      kubernetes.azure.com/agentpool: userpool

frankgateway:
  nodeSelector:
    kubernetes.azure.com/agentpool: userpool
  tls:
    sslSync:
      nodeSelector:
        kubernetes.azure.com/agentpool: userpool
  etcd:
    nodeSelector:
      kubernetes.azure.com/agentpool: userpool
  seed:
    job:
      nodeSelector:
        kubernetes.azure.com/agentpool: userpool
  clientCertSync:
    nodeSelector:
      kubernetes.azure.com/agentpool: userpool
  dashboard:
    nodeSelector:
      kubernetes.azure.com/agentpool: userpool
    auth:
      oauth2Proxy:
        nodeSelector:
          kubernetes.azure.com/agentpool: userpool
      shim:
        nodeSelector:
          kubernetes.azure.com/agentpool: userpool

openbao:
  configuration:
    job:
      nodeSelector:
        kubernetes.azure.com/agentpool: userpool
  database:
    schemaJob:
      nodeSelector:
        kubernetes.azure.com/agentpool: userpool
```

### Verify after deploy

Check that no PodiumD pod runs outside the `userpool`:

```sh
kubectl get pods -n podiumd -o wide
kubectl get nodes -L kubernetes.azure.com/agentpool
```

Every pod in the `NODE` column must be on a node with agentpool `userpool`.
Then check in Grafana that logs of these components are visible.
