# Upgrade guide: PodiumD 4.8.5 → 4.9.0

> See the Confluence Releases page for the agreed application
> targets: <https://dimpact.atlassian.net/wiki/spaces/PCP/pages/7602191/Releases+PodiumD>.

This is the upgrade guide for environments already on **4.8.5** (the current
stable baseline). Most changes below ship entirely in the chart's own
defaults, but **KISS 3.0.0 requires `podiumd.yml` edits** (Enterprise Search
removed) and **ITA 3.3.0 needs an edit only if you override
`ita.poller.schedule`/`ita.poller.notification`** — see the component
sections below.

## Component versions (4.9.0 vs 4.8.5)

| Component | App version | Helm chart | Notes |
| --- | --- | --- | --- |
| ZAC (Zaakafhandelcomponent) | 5.0.2 → 5.1.0 | 1.0.297 (unchanged) | ACR mirror only — see note below |
| ZGW Office Add-in (frontend + backend) | v0.9.313 → v0.9.352 | 0.0.89 (unchanged) | ACR mirror only — see note below |
| ClamAV | 1.5.2 → 1.5.4 | 3.7.1 → 3.7.2 | ACR mirror only |
| Kiss Elasticsearch | 8.19.3 → 8.19.19 | — | ACR mirror only (ECK-managed) |
| Kiss Kibana | 8.19.3 → 8.19.19 | — | ACR mirror only (ECK-managed) |
| Kiss Enterprise Search | 8.19.3 → 8.19.19 | — | ACR mirror only (ECK-managed) |
| ECK Operator | 3.4.0 → 3.5.0 | 3.4.0 → 3.5.0 | CRD schema change, additive-only — see [operators & CRDs](4.8.5-to-4.9.0-operators-crds.md) |
| ECK Stack (kiss-eck) | — | 0.19.0 → 0.20.0 | No CRDs of its own |
| Keycloak | 26.6.4 → 26.7.2 | 1.12.1 (unchanged) | App image only — operator stays put, see note below |
| Python (ensurePodiumdAdminUser init image) | 3.14-slim → 3.14.7-slim | — | Now pinned to a specific patch instead of the floating minor tag |
| KISS | 2.2.4 → 3.0.0 | 2.2.4 → 3.0.0 | **action required** — Enterprise Search removed, `podiumd.yml` edits needed |
| PodiumD Adapter | 0.6.6 → 0.6.7  | - | ACR Mirror only — see note below |
| ITA | 3.2.0 → 3.3.0 | 3.2.0 → 3.3.0 | poller split into two CronJobs — see note below; values migration needed if `ita.poller` overrides are present |
| PABC | 1.1.0 → 1.1.1  | 1.1.0 → 1.1.1 | ACR Mirror only — see note below |
| Redis (redis-ha) | 8.6.2 → 8.6.6 | — | ACR mirror only — same 8.6.x branch, security patches |
| redis-exporter | 1.82.0 → 1.89.0 | — | ACR mirror only — sidecar disabled by default |
| redis-operator | 0.25.0 → 0.26.0 | 0.25.0 → 0.26.1 | ACR mirror only — see note below |
| openformulieren | 3.4.10 → 3.5.6 | 1.12.0 (unchanged) | - |
| openinwoner | 2.4.2 → 2.4.3 | 2.4.0 (unchanged) | - |

## Changes

### ZAC 5.0.2 → 5.1.0 (chart 1.0.297, unchanged)

PodiumD 4.9.0 upgrades **ZAC (Zaakafhandelcomponent)** from app version 5.0.2
to 5.1.0. The Helm chart stays on `1.0.297` — that chart line was already
bumped ahead of the image in the 4.8.5 hop (tested upstream against app
5.5.0, image deliberately held back); 4.9.0 simply advances the image within
that same chart line instead of bumping the chart again. See the
[4.8.4 → 4.8.5 guide](4.8.4-to-4.8.5-upgrade.md#zac-501--502-helm-chart-10251--10297)
for why the chart and image versions were decoupled in the first place.

- Image tag pin `zac.image.tag` `5.0.2` → `5.1.0` in
  `charts/podiumd/values.yaml`. No `Chart.yaml` change.
- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.** The only chart-level change is the ZAC
image tag, which every environment inherits from the chart default (gemeente
`podiumd.yml` files override `registry`/`repository` for their own ACR mirror
but never `tag`). The ZAC sidecar images (nginx-unprivileged, curl, busybox,
gotenberg, opa) are unchanged from 4.8.5.

> `zac.brpApi.protocollering.verwerking.extendWithZaaktype` (introduced in the
> 4.8.5 hop) is unchanged in 5.1.0; nothing to redo here. For application-level
> behaviour changes between ZAC 5.0.2 and 5.1.0, see the ZAC release notes.

### ZGW Office Add-in v0.9.313 → v0.9.352 (chart 0.0.89, unchanged)

PodiumD 4.9.0 upgrades the **ZGW Office Add-in** frontend and backend from
`v0.9.313` to `v0.9.352`. The Helm chart version remains `0.0.89`. This
image was not touched by the 4.8.4 or 4.8.5 hops, so `v0.9.313` is still
what a 4.8.5 environment has today.

- Image tag pins `zgw-office-addin.frontend.image.tag` and
  `zgw-office-addin.backend.image.tag` updated in
  `charts/podiumd/values.yaml`.
- Image / digests: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.** As with ZAC, the tags come from the chart
defaults; environments only override `registry`/`repository`. The repository
names (`zgw-office-addin-{frontend,backend}`, hyphen already dropped) are
unchanged from 4.8.5.

### KISS 2.2.4 → 3.0.0 — Enterprise Search removed

KISS 3.0.0 no longer uses Enterprise Search. It queries the Elasticsearch
`search-*` indices directly, and websites are crawled by the **Elastic Open
Crawler**, which runs as a CronJob per site and writes straight to
Elasticsearch. A `pre-install`/`pre-upgrade` hook (`crawler-index-template`)
registers the `kiss-crawler-website` index template first.

There is **no migration path** and no data migration: existing indices, the
Elasticsearch StatefulSet and its PVCs are untouched. Enterprise Search may be
left running; KISS simply stops using it. See the
[KISS changelog](https://github.com/Klantinteractie-Servicesysteem/KISS-frontend/blob/main/docs/changelog/changelog.md).

Verified on test00 (podiumd 4.8.0 → this branch): StatefulSet UID and all three
PVCs unchanged, index template registered, crawler CronJob completed without
errors.

**Three things bite in practice. All three were hit on test00:**

1. **`settings.syncJobs.website.domain` must not contain a path, not even a trailing slash.** The Open
   Crawler rejects it where the Enterprise Search crawler accepted it:

   ```
   ArgumentError: Domain "https://www.example.nl/" cannot have a path
   ```

   Strip the trailing slash from every `kiss.settings.syncJobs.website[].domain`
   in `podiumd.yml`. The CronJob crashloops otherwise; the deploy itself stays
   green, so this is easy to miss.

2. **The Elasticsearch password must be correct before you upgrade.** The
   index-template hook runs `pre-upgrade` and fails the whole release on a bad
   credential:

   ```
   curl: (22) The requested URL returned error: 401
   Error: UPGRADE FAILED: pre-upgrade hooks failed: resource Job/podiumd/contact-crawler-index-template not ready
   ```

   `kiss.settings.elastic.password` must match the ECK-generated password in
   Secret `kiss-es-elastic-user`. Until 3.0.0 a stale value only broke the sync
   jobs silently (crashlooping jobs, `search-*` indices at 0 documents); now it
   blocks the deploy. Check before upgrading:

   ```bash
   PW=$(kubectl get secret -n podiumd kiss-es-elastic-user -o go-template='{{.data.elastic|base64decode}}')
   printf 'user = "elastic:%s"\n' "$PW" | kubectl exec -i -n podiumd kiss-es-default-0 -c elasticsearch -- \
     curl -s -k -K - -o /dev/null -w '%{http_code}\n' "https://localhost:9200/_cat/health"
   ```

3. **Three images move.** 
   
   - `kiss-elastic-sync` now follows the chart version
   (`0.3.3` → `3.0.0`). 
   -  `docker.elastic.co/integrations/crawler:1.0.0` is the 
   new image for the Elastic Open Crawler. 
   - And KISS uses Curl to create the 
   indexes for Websites, so this image is also part of the KISS values.
   
   The kiss chart does **not** honour `global.imageRegistry`, so
   environments that pin `kiss.image.repository` to the ACR mirror must also
   pin `syncJobs.crawlerImage.repository` and
   `syncJobs.indexTemplateImage.repository`, or Azure Policy blocks the pods.

**NOTE: `podiumd.yml` action required:** remove the `kiss.settings.enterpriseSearch`
block (`baseUrl`, `engine`, `publicApiKey`, `privateApiKey`). The keys no
longer exist in the subchart. The matching key vault secrets become unused.

### PodiumD Adapter 0.6.6 → 0.6.7 

PodiumD 4.9.0 upgrades the **PodiumD Adapter** from
`v0.6.6` to `v0.6.7`. 

- Image tag pins `kiss.adapter.image.tag` updated in
  `charts/podiumd/values.yaml`.
- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.** 

### ITA 3.2.0 → 3.3.0 (chart 3.2.0 → 3.3.0)

PodiumD 4.9.0 upgrades **ITA (Interne Taak Afhandeling)** from app version 3.2.0
to 3.3.0.

- Helm chart `internetaakafhandeling` `3.2.0` → `3.3.0` in
  `charts/podiumd/Chart.yaml`.
- Image tags `ita.web.image.tag` and `ita.poller.image.tag` `3.2.0` → `3.3.0` in
  `charts/podiumd/values.yaml`.
- Image / digests: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

#### Poller split into two CronJobs

In 3.3.0 the single `poller` CronJob is replaced by two separate CronJobs:

- **`nieuweInternetaakNotificatie`** — notifies handlers on new internet tasks.
  The `schedule` and `notification` keys move from `ita.poller` to
  `ita.nieuweInternetaakNotificatie`.
- **`verlopenContactverzoekHerinneringNotificatie`** — new daily reminder
  (weekdays at 07:00) for expired contact requests. Enabled by default.

Environments that override `ita.poller.schedule` or `ita.poller.notification` in
`podiumd.yml` must move those keys to `ita.nieuweInternetaakNotificatie`. Without
the migration the overrides silently fall back to chart defaults. See
[`4.8.5-to-4.9.0-values-deltas.md`](4.8.5-to-4.9.0-values-deltas.md#ita-3200--3300--poller-split).

If your environment does **not** override `ita.poller.schedule` or
`ita.poller.notification`, no `podiumd.yml` changes are needed.

#### Application changes (ITA 3.3.0)

From the [ITA changelog](https://github.com/Interne-Taak-Afhandeling/ITA/blob/main/docs/changelog/changelog.md):

- Reliable forwarding to department/group or to an individual employee
- Contact request access restricted to own department/group
- Daily e-mail reminder for employees and departments on expired contact requests
- Clear action and status labels in contact request handling
- Worklist urgency status and improved columns
- Department/group shown on a contact request



### PABC v1.1.0 → v1.1.1 (chart v1.1.0 → v1.1.1)

PodiumD 4.9.0 upgrades **PABC** `v1.1.0` to `v1.1.1`. This includes both the `pabc-api` and the `pabc-migrations`. The Helm chart version also bumps from `v1.1.0` to `v1.1.1`.

- Image tags are pinned, `pabc.image.tag` and `pabc.migrations.image.tag` updated in
  `charts/podiumd/values.yaml`.
- Image / digests: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.** 

### ClamAV 1.5.2 → 1.5.4 (chart 3.7.1 → 3.7.2)

Image tag pin `clamav.image.tag` in `charts/podiumd/values.yaml`, plus the
`clamav` Helm chart dependency version. No config or schema changes.

- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### Kiss's ECK-managed Elasticsearch/Kibana/Enterprise Search 8.19.3 → 8.19.19

16 patches on the same 8.19.x branch, applied to all three
`kiss-eck.eck-elasticsearch` / `eck-kibana` / `eck-enterprise-search`
`version:` fields in lockstep. Elastic 8.x end-of-maintenance is
2027-01-15, end-of-support 2027-07-15.

- Image / digests: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### ECK Operator 3.4.0 → 3.5.0 + ECK Stack (kiss-eck) 0.19.0 → 0.20.0

The `eck-operator` chart bump also moves its bundled operator image from
`3.4.0` to `3.5.0` (the chart's `image.tag` floats on `appVersion`, and
podiumd doesn't override it). The CRD schema change was verified
additive-only — see [operators & CRDs](4.8.5-to-4.9.0-operators-crds.md) for
the full diff method and results. The `eck-stack` (alias `kiss-eck`) chart
bump to `0.20.0` ships no image of its own.

- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### Keycloak app image 26.6.4 → 26.7.2

App image only — `keycloak.image.tag` in `charts/podiumd/values.yaml`. The
`keycloak-operator` chart stays on `1.12.1`: no operator release yet supports
Keycloak 26.7.x. The operator's own `RELATED_IMAGE_KEYCLOAK` reference
(`keycloak-operator.config.keycloakImage`) is kept in sync with the app image
per the requirement documented in that block's `values.yaml` comment.

- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### Python (ensurePodiumdAdminUser init image) 3.14-slim → 3.14.7-slim

The PBKDF2-hash init container for the `ensurePodiumdAdminUser` job now pins
a specific patch (`3.14.7-slim`) instead of the floating `3.14-slim` minor
tag. No behavior change expected — same Python minor line.

- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### Redis (redis-ha) 8.6.2 → 8.6.6

Same `8.6.x` branch — `redis-operator.redis-ha.image.tag` in
`charts/podiumd/values.yaml`, no chart change. Picks up several upstream
security fixes across 8.6.3/8.6.5/8.6.6, including RCE-class issues in
`RESTORE`/Lua and a TLS client-certificate authentication bypass. No config
directive or protocol changes — Redis patch releases don't break compatibility.

- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### redis-exporter 1.82.0 → 1.89.0

`redis-operator.redis-ha.redisExporter.image.tag` in `charts/podiumd/values.yaml`.
Purely additive upstream releases (new metrics/flags, one crash fix); no
removed metrics or flags. The sidecar is disabled by default
(`redisExporter.enabled: false`, turned on via `values-enable-observability.yaml`).

- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### redis-operator 0.25.0 → 0.26.0 (chart 0.25.0 → 0.26.1)

Both `redis-operator.redisOperator.imageTag` and the new
`redis-operator.redisOperator.initContainerImageTag` override in
`charts/podiumd/values.yaml`, plus the `redis-operator` Helm chart dependency
version in `Chart.yaml`. CRD diff between chart 0.25.0 and 0.26.1 verified as
**zero** — no schema change. The only `values.yaml` shape change upstream is
`featureGates` moving from a fixed map to `{}` with commented examples; our
explicit `redis-operator.featureGates.GenerateConfigInInitContainer: true`
gate is unaffected and remains supported.

- Image / digests: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

> **Note for chart maintainers:** the operator bug tracked in
> [`docs/apps/redis/redis-ha.md`](../apps/redis/redis-ha.md) (PR
> [#1720](https://github.com/OT-CONTAINER-KIT/redis-operator/pull/1720),
> the reason `redis-operator.redis-ha.labelMasterCronJob` exists) has in fact
> been fixed since redis-operator `0.25.0` — i.e. already true on 4.8.5, not
> just as of this bump. The label-master CronJob workaround is a candidate for
> removal, but flip `labelMasterCronJob.enabled: false` only after verifying
> the operator self-heals correctly on a simultaneous pod restart in a test
> environment — this hop does not change that workaround's default.

### openformulieren 3.4.10 → 3.5.6 (chart 1.12.0, unchanged)

PodiumD 4.9.0 upgrades **openformulieren** from app version 3.4.10
to 3.5.6.

- Image tag pin `openformulieren.image.tag` `3.4.10` → `3.5.6` in
  `charts/podiumd/values.yaml`.
- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

### openinwoner 2.4.2 → 2.4.3 (chart 2.4.0, unchanged)

PodiumD 4.9.0 upgrades **openinwoner** from app version 2.4.2
to 2.4.3.

- Image tag pin `openinwoner.image.tag` `2.4.2` → `2.4.3` in
  `charts/podiumd/values.yaml`.
- Image / digest: see [`images-4.9.0.yaml`](../images/images-4.9.0.yaml).

**No `podiumd.yml` action required.**

## Per-environment checklist

Read the whole guide once first. Then, for **every environment** you upgrade,
work through this list top to bottom. Most components need no gemeente-values
edits, but check whether your environment needs the KISS 3.0.0 or ITA 3.3.0
edits — see [`4.8.5-to-4.9.0-values-deltas.md`](4.8.5-to-4.9.0-values-deltas.md).

### A. Prepare

- [ ] **ACR mirror**: mirror the full 4.9.0 image set under the strip-registry
      names ([`images-4.9.0.yaml`](../images/images-4.9.0.yaml)) — ZAC `5.1.0`,
      ZGW Office Add-in `v0.9.352` frontend/backend, ClamAV `1.5.4`, Keycloak
      `26.7.2`, ECK Operator `3.5.0`, Kiss's Elasticsearch/Kibana/Enterprise
      Search `8.19.19`, Python `3.14.7-slim`, Redis `8.6.6`, redis-exporter
      `1.89.0`, and redis-operator `0.26.0`.
- [ ] Read [operators & CRDs](4.8.5-to-4.9.0-operators-crds.md) — the ECK
      Operator bump carries a CRD schema change (additive-only, no adoption
      step needed, but read it before the deploy).

### B. Deploy

- [ ] Run the deploy pipeline / `helm upgrade`.
