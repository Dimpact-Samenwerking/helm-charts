# Values deltas — PodiumD 4.8.5 → 4.8.6

**No gemeente `podiumd.yml` changes are required for this hop.**

## No action needed (informational)

- **Keycloak Operator**: `keycloak-operator.operator.image.sha` was added — this is podiumd's own
  chart-default override, not a gemeente-facing key. It pins the Keycloak
  Operator image digest explicitly because the adfinis 1.13.0 chart's
  bundled default `operator.image.sha` doesn't match any manifest published
  under the `26.7.2` tag. See
  [`4.8.5-to-4.8.6-upgrade.md`](4.8.5-to-4.8.6-upgrade.md) for the full
  double-digest caveat this works around.
- Keycloak server + operator image tags moved `26.6.4` → `26.7.2`; these are
  podiumd chart defaults, not values a gemeente `podiumd.yml` overrides.
