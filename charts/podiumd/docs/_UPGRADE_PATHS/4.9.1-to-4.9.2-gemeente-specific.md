# Gemeente-specific notes — PodiumD 4.9.1 → 4.9.2

Findings for this hop that apply to a **specific gemeente or environment** —
not to the release in general — are collected here: data quirks, local
overrides, hosting particulars, incident follow-ups.


## Gemeenten that have activated KISS, on `accp` and `prod`

In KISS 3.x, in PodiumD 4.9.x, KISS is abandoning Elastic Enterprise Search. 
With this change, the way KISS queries Elasticsearch has also changed.
Where this used to be done via a meta-engine in Enterprise Search, KISS now
queries the Elasticsearch indices directly. 
As a consequence all indices now show up in the interface, even if they 
contain no data. And the KISS helm-chart creates an index for every
syncJob in the helm-values. 

The sources that are indexed vary per gemeente, especially where Kennisbank
and VAC are cocerned. Some use Kennisbank not VAC, some use VAC not Kennisbank.
And some don't use either of them, but use only a website or also a Sharepoint
crawler. 

PodiumD and KISS has by default been enabled for most gemeenten, also with 
three syncJobs defined: Kennisbank, VAC and Smoelenboek. To prevent the empty
indices to show up in the interface, the values for each Gemeente that has 
activated Contact should match the actual search sources that gemeente uses. 
And before deploy the obsolete indices should be deleted. 

The steps described below should be executed on both ACCP and PROD environments 
of each gemeente.

Below that is a list, per gemeente, of the actual sources they use, the indices 
that need to deleted, and the correct values-snippet for `podiumd.yml` **on Acceptance**

### Steps to follow

1. Delete the indicated indices:
   a. Change the Kubectl context to the context of the gemeente in question
   b. For each index, run the script [`charts/podiumd/scripts/delete-es-index.sh`](../../scripts/delete-es-index.sh) <br/> 
   For instance `./<path-to-the-script>/delete-es-index.sh "search-vac" `
2. Update the values for `kiss.settings.syncjobs` in `podiumd.yml` before deploying PodiumD 4.9.2
   

### Gemeente Assen

* Sources they use: VAC and website Assen
* index to be deleted : `search-kennisbank`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      crawlerImage:
        repository: "acrprodmgmt.azurecr.io/integrations/crawler"
      indexTemplateImage:
        repository: "acrprodmgmt.azurecr.io/curlimages/curl"
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.assen.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      vac:
        manageFromKiss: true
        baseUrl: "http://objecten.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.assen.nl/api/v2/objecttypes/REP_CONTACT_VAC_UUID_REP"
        objectTypeVersion: 2
        schedule: "15 6-14 * * 1-5" # OTA waarde
        token: "REP_OBJECTEN_CREDENTIALS_CONTACT_TOKEN_REP"
      website:
        - sourceName: "Website Assen"
          domain: "https://www.assen.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
```

### Gemeente Deventer

* Sources they use: Kennisbank, Smoelenboek, Deventer website
* index to be deleted : `search-vac`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      kennisbank:
        baseUrl: "http://objecten.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-podiumd-objecttypen.deventer.nl/api/v2/objecttypes/REP_CONTACT_KENNISARTIKEL_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        token: "REP_OBJECTEN_CREDENTIALS_CONTACT_TOKEN_REP"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-podiumd-objecttypen.deventer.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      website:
        - sourceName: "Website"
          domain: "https://www.deventer.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
```

### Gemeente Enschede

* Sources they use: VAC, Smoelenboek, Enschede website, Losser website
* index to be deleted : `search-kennisbank`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      crawlerImage:
        repository: "acrprodmgmt.azurecr.io/integrations/crawler"
      indexTemplateImage:
        repository: "acrprodmgmt.azurecr.io/curlimages/curl"
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.enschede.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      vac:
        manageFromKiss: true
        baseUrl: "http://objecten.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.enschede.nl/api/v2/objecttypes/REP_CONTACT_VAC_UUID_REP"
        objectTypeVersion: 2
        schedule: "15 6-14 * * 1-5" # OTA waarde
        token: "REP_OBJECTEN_CREDENTIALS_CONTACT_TOKEN_REP"
      website:
        - sourceName: "Website Enschede"
          domain: "https://www.enschede.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
        - sourceName: "Website Losser"
          domain: "https://www.losser.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
```


### Gemeente Gemert-Bakel 

* Sources they use: Smoelenboek, Website Gemert-Bakel
* index to be deleted : `search-kennisbank` AND `search-vac`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.gemert-bakel.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      website:
        - sourceName: "Website Gemert-Bakel"
          domain: "https://www.gemert-bakel.nl/"
          schedule: "0 22 * * 1-5" # OTA waarde
```


### Gemeente Kampen

* Sources they use: Smoelenboek, VAC, Sharepoint: Kennisbank KCC, website kampen.nl
* index to be deleted : `search-kennisbank`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.kampen.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      vac:
        manageFromKiss: true
        baseUrl: "http://objecten.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.kampen.nl/api/v2/objecttypes/REP_CONTACT_VAC_UUID_REP"
        objectTypeVersion: 1
        schedule: "15 6-14 * * 1-5" # OTA waarde
        token: "REP_OBJECTEN_CREDENTIALS_CONTACT_TOKEN_REP"
      sharepoint:
        - clientId: "REP_CONTACT_SHAREPOINT_CLIENTID_REP"
          sourceName: "Kennisbank KCC"
          clientSecret: "REP_CONTACT_SHAREPOINT_SECRET_REP"
          tenantId: "REP_CONTACT_SHAREPOINT_TENANT_REP"
          siteUrl: "https://gemeentekampen.sharepoint.com/sites/nieuwekennisbankKCC"
          historyLimit: 1
      website:
        - sourceName: "Website"
          domain: "https://www.kampen.nl"
          schedule: "0 22 * * *"
```


### Gemeente Laarbeek: 

* Sources they use: Smoelenboek, Website laarbeek.nl
* index to be deleted : `search-kennisbank` AND `search-vac`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.laarbeek.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      website:
        - sourceName: "Website"
          domain: "https://www.laarbeek.nl"
          schedule: "0 22 * * *"
```

### Gemeente Oldenzaal
* Sources they use: Smoelenboek, Website oldenzaal.nl
* index to be deleted : `search-kennisbank` AND `search-vac`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.oldenzaal.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      website:
        - sourceName: "Website"
          domain: "https://www.oldenzaal.nl"
          schedule: "0 22 * * *"
```

### Gemeente Oldambt

* Sources they use: Smoelenboek, VAC, Website oldambt.nl
* index to be deleted : `search-kennisbank`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.gemeente-oldambt.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      vac:
        manageFromKiss: true
        baseUrl: "http://objecten.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.gemeente-oldambt.nl/api/v2/objecttypes/REP_CONTACT_VAC_UUID_REP"
        objectTypeVersion: 1
        schedule: "15 6-14 * * 1-5" # OTA waarde
        token: "REP_OBJECTEN_CREDENTIALS_CONTACT_TOKEN_REP"
      website:
        - sourceName: "Website"
          domain: "https://www.gemeente-oldambt.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
``` 

### Gemeente Oost-Gelre

* Sources they use: Smoelenboek, VAC en website Oost Gelre
* index to be deleted : `search-kennisbank` 
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.oostgelre.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      vac:
        manageFromKiss: false
        baseUrl: "http://objecten.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.oostgelre.nl/api/v2/objecttypes/REP_CONTACT_VAC_UUID_REP"
        objectTypeVersion: 1
        schedule: "15 6-14 * * 1-5" # OTA waarde
        token: "REP_OBJECTEN_CREDENTIALS_CONTACT_TOKEN_REP"
      website:
        - sourceName: "Website"
          domain: "https://www.oostgelre.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
```

### Gemeente Zwolle

* Sources they use: Smoelenboek, Sharepoint: Kennisbank IZ,  Website Zwolle
* index to be deleted : `search-kennisbank`, `search-vac` AND `search-podiumd-component`
* snippet for `kiss.settings.syncjobs` in `podiumd.yml` :

```
    syncJobs:
      crawlerImage:
        repository: "acrprodmgmt.azurecr.io/integrations/crawler"
      indexTemplateImage:
        repository: "acrprodmgmt.azurecr.io/curlimages/curl"
      image:
        repository: "acrprodmgmt.azurecr.io/klantinteractie-servicesysteem/kiss-elastic-sync"
      medewerkers:
        baseUrl: "http://podiumd-adapter.podiumd.svc.cluster.local"
        objectTypeUrl: "https://acc-objecttypen.zwolle.nl/api/v2/objecttypes/REP_CONTACT_MEDEWERKER_UUID_REP"
        schedule: "15 6-14 * * 1-5" # OTA waarde
        clientId: "contact_intern"
        clientSecret: "REP_CONTACT_ADAPTER_SECRET_REP"
      sharepoint:
        - clientId: "REP_CONTACT_SHAREPOINT_CLIENTID_REP"
          sourceName: "Kennisbank IZ"
          clientSecret: "REP_CONTACT_SHAREPOINT_SECRET_REP"
          tenantId: "REP_CONTACT_SHAREPOINT_TENANT_REP"
          siteUrl: "https://gemeentezwolle.sharepoint.com/sites/KennisbankInwonerszaken"
          historyLimit: 0
      website:
        - sourceName: "Website"
          domain: "https://www.zwolle.nl"
          schedule: "0 22 * * 1-5" # OTA waarde
```