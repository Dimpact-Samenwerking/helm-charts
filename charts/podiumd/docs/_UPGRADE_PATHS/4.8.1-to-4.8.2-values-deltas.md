# Values deltas — PodiumD 4.8.1 → 4.8.2

No gemeente `podiumd.yml` changes are required for this hop — the update needed for the
objecten subchart bump (2.12.0 → 2.12.1) ships in the chart `values.yaml` defaults.

