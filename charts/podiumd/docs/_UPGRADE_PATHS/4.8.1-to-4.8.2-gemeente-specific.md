# Gemeente-specific notes — PodiumD 4.8.1 → 4.8.2

Findings for this hop that apply to a **specific gemeente or environment** —
not to the release in general — are collected here: data quirks, local
overrides, hosting particulars, incident follow-ups.

## Rotterdam (accp, test)

**Unaffected by the 4.8.2 nginx-unprivileged bump — no action, but be aware:**

- `rott/accp/podiumd.yml` and `rott/test/podiumd.yml` pin an explicit
  `tag: "1.29.8"` for `nginx-unprivileged`, on both the ZAC nginx sidecar
  and apiproxy.
- That pin is *older* than even the pre-4.8.2 chart default (`1.31.1`) and
  predates the 4.8.2 `1.31.1 → 1.31.3` bump entirely.
- An explicit tag override always wins over the chart default. These two
  environments therefore stay on `1.29.8` — before and after this hop —
  until their own `podiumd.yml` is updated.

## ZAC — BRP protocollering configuration (per gemeente)

ZAC 5.0.1 replaced the single `protocollering.aanbieder` vendor selector with
an explicit, field-per-dimension structure: each gemeente configures the exact
HTTP headers and values its BRP gateway vendor expects, instead of ZAC
inferring them from a vendor name.

Two things to know before configuring:

- Protocollering is **disabled by default** (`protocollering.enabled: false`).
  Enable it only when the BRP gateway requires it.
- All values below go in the gemeente's `podiumd.yml` under `zac.brpApi`.

The per-vendor blocks below are known-good configurations per gemeente.

### iConnect — Assen, Groningen

```yaml
zac:
  brpApi:
    url: "http://api-proxy.podiumd.svc.cluster.local/brp"
    apiKey:
      header: "x-api-key"
      value: "REP_API_BRP_KEY_REP"
    logLevel: "OFF"
    protocollering:
      enabled: true
      systemUser: "SystemUser"
      originOin:
        oin: "<de originele originOin waarde>"
        header: "x-origin-oin"
      doelbinding:
        perZaaktype: true
        header: "x-doelbinding"
        zoekmet: "BRPACT-ZoekenAlgemeen"
        raadpleegmet: "BRPACT-ZacPersoonBasis"
      verwerking:
        header: "x-verwerking"
        register: "DigitaleDienstverlening"
      gebruiker:
        header: "x-gebruiker"
      toepassing:
        header: "x-toepassing"
        value: "ZAC"
```

### eServices — Oldambt

```yaml
zac:
  brpApi:
    url: "http://api-proxy.podiumd.svc.cluster.local/brp"
    apiKey:
      header: "x-api-key"
      value: "REP_API_BRP_KEY_REP"
    logLevel: "OFF"
    protocollering:
      enabled: true
      originOin:
        oin: "00000001821479490000"  ## overgenomen van kiss.configuration.settings.haalcentraal.customHeaders
        header: "x-request-organization"
      doelbinding:
        perZaaktype: false
        header: ""
      verwerking:
        header: "x-request-afnemerscode"
        register: "510225" ## overgenomen van kiss.configuration.settings.haalcentraal.customHeaders
      gebruiker:
        header: "x-request-user"
      toepassing:
        header: "x-request-application"
        value: "oldambt-acc-zac-podiumd-zaak"
```

> **Oldambt + ApiProxy toepassingsheader (PodiumD 4.8.x):** Oldambt routes
> BRP and KvK requests from Open Formulieren through ApiProxy, so ApiProxy
> must keep injecting a toepassing header — for Open Formulieren, not for
> ZAC. Do **not** disable `toepassingHeaderName` for Oldambt; check the
> values to use in
> [DS-5918](https://dimpact.atlassian.net/browse/DS-5918).

### 2Secure / EnableU — Enschede

```yaml
zac:
  brpApi:
    url: "http://api-proxy.podiumd.svc.cluster.local/brp"
    apiKey:
      header: "x-api-key"
      value: "REP_API_BRP_KEY_REP"
    logLevel: "OFF"
    protocollering:
      enabled: true
      systemUser: "SystemUser"
      doelbinding:
        perZaaktype: false
      verwerking:
        header: "x-verwerking"
        register: "Klantcontactcentrum"
      gebruiker:
        header: "x-gebruiker"
      toepassing:
        header: "x-applicatie"
        value: "ZAC"
```

### 2Secure — Zwolle, Kampen, Oldenzaal

```yaml
zac:
  brpApi:
    url: "http://api-proxy.podiumd.svc.cluster.local/brp"
    apiKey:
      header: "x-api-key"
      value: "REP_API_BRP_KEY_REP"
    logLevel: "OFF"
    protocollering:
      enabled: true
      systemUser: "SystemUser"
      doelbinding:
        perZaaktype: false
      verwerking:
        header: ""
      gebruiker:
        header: "x-gebruiker"
      toepassing:
        header: "x-applicatie"
        value: "ZAC"
```

### Yenlo — Oost Gelre

```yaml
zac:
  brpApi:
    url: "http://api-proxy.podiumd.svc.cluster.local/brp"
    apiKey:
      header: "x-api-key"
      value: "REP_API_BRP_KEY_REP"
    logLevel: "OFF"
    protocollering:
      enabled: true
      systemUser: "SystemUser"
      doelbinding:
        perZaaktype: false
      verwerking:
        header: ""
      gebruiker:
        header: "x-gebruiker"
      toepassing:
        header: "x-applicatie"
        value: "ZAC"
```

### API-proxy: disable `toepassingHeaderName` when protocollering is enabled

The `apiproxy` component has a `brp.toepassingHeaderName` setting that controls
the toepassing header forwarded to the BRP gateway. When set, the api-proxy
uses a pass-through-with-fallback pattern (nginx `map`): it forwards the header
value if one is sent, or injects `toepassingDefaultValue` if it is not.

With ZAC 5.0.1 protocollering enabled, ZAC sends the toepassing header itself
and the api-proxy forwards ZAC's value. To keep the configuration explicit —
one component clearly owns the header — **disable the api-proxy injection** by
setting `toepassingHeaderName` to `""`:

```yaml
# 4.7.x iConnect config — toepassingHeaderName was used as a fallback/injector
apiproxy:
  locations:
    brp:
      toepassingHeaderName: "X-Toepassing"
      toepassingDefaultValue: "ZAC"

# 5.0.1 — ZAC owns the header via protocollering; disable api-proxy injection
apiproxy:
  locations:
    brp:
      toepassingHeaderName: ""
```

Notes:

- With `toepassingHeaderName: ""`, the nginx `map` and `proxy_set_header`
  blocks are not rendered at all; ZAC's `x-toepassing` header passes through
  via nginx's default header forwarding.
- For eServices and 2Secure environments `toepassingHeaderName` is typically
  already `""` — no change needed.
- Exception: Oldambt keeps ApiProxy header injection enabled for
  Open Formulieren — see the note under [eServices — Oldambt](#eservices--oldambt).

### Field reference

| Field | Required | Description |
|---|---|---|
| `brpApi.logLevel` | no | Log level for BRP API calls. `"OFF"` suppresses request/response logging. |
| `protocollering.enabled` | yes | Enable or disable BRP protocollering. |
| `protocollering.systemUser` | yes | Fixed username sent as the acting system user. |
| `protocollering.originOin.oin` | when enabled | The gemeente's OIN, sent to identify the requesting organisation. |
| `protocollering.originOin.header` | when enabled | HTTP header name for the OIN. |
| `protocollering.doelbinding.perZaaktype` | yes | When `true`, ZAC derives the doelbinding from the zaaktype. When `false`, a fixed doelbinding header is sent (or omitted if `header` is empty). |
| `protocollering.doelbinding.header` | when `perZaaktype: true` | HTTP header name for the doelbinding value. Set to `""` to omit the header. |
| `protocollering.doelbinding.zoekmet` | iConnect | BRP action code for search operations. |
| `protocollering.doelbinding.raadpleegmet` | iConnect | BRP action code for retrieval operations. |
| `protocollering.verwerking.header` | when enabled | HTTP header name for the verwerking (processing) identifier. |
| `protocollering.verwerking.register` | when enabled | Name of the verwerkingsregister (processing register). |
| `protocollering.gebruiker.header` | when enabled | HTTP header name for the acting end-user. ZAC fills this with the logged-in user. |
| `protocollering.toepassing.header` | when enabled | HTTP header name for the application identifier. |
| `protocollering.toepassing.value` | when enabled | Fixed application identifier value sent in the header. |

### Migration from ZAC 4.7.x

The old `protocollering.aanbieder` field and its implicit vendor-specific
defaults are gone in ZAC 5.0.1. Map previous configuration as follows:

| Old (4.7.x) | New (5.0.1) |
|---|---|
| `protocollering.aanbieder: "iConnect"` | `protocollering.enabled: true` + full iConnect block above |
| `protocollering.aanbieder: "2Secure"` | `protocollering.enabled: true` + full 2Secure block above |
| `protocollering.aanbieder: ""` | `protocollering.enabled: false` |
| `protocollering.verwerkingsregister: "..."` | `protocollering.verwerking.register: "..."` |
| `protocollering.doelbinding.zoekmet` | `protocollering.doelbinding.zoekmet` (unchanged) |
| `protocollering.doelbinding.raadpleegmet` | `protocollering.doelbinding.raadpleegmet` (unchanged) |

<!-- Add entries per gemeente/environment:

## <gemeente> (<env>)

- What was hit, why it is specific to this environment, and the
  fix/workaround applied.
-->
