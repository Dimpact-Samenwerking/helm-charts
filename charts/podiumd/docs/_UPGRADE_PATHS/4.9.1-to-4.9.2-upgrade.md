# Upgrade guide: PodiumD 4.9.1 → 4.9.2

> See the Confluence Releases page for the agreed application
> targets: <https://dimpact.atlassian.net/wiki/spaces/PCP/pages/7602191/Releases+PodiumD>.

## Component versions (4.9.2 vs 4.9.1)

| Component | App version | Helm chart | Notes |
| --- | --- | --- | --- |
| postgres | 16-alpine → 16.15-alpine | - | - |

## Changes

### postgres 16-alpine → 16.15-alpine

PodiumD 4.9.2 upgrades the shared **postgres** image to 16.15-alpine,
pinned at:

- `global.images.postgres.tag` `16-alpine` → `16.15-alpine`

- Image / digest: see [`images-4.9.2.yaml`](../images/images-4.9.2.yaml).

### PABC: realm roles per functional role (IN-2004)

ZAC 5.4.x sends the user's Keycloak **realm roles** (`realm_access.roles`) to
PABC as functional roles, not the group names from `group_membership`. With
only groups, PABC returns no application roles and every user sees
"u heeft geen toestemming om deze pagina te bekijken".

- With `keycloak.config.skipRoles`/`skipGroups: false` the realm import now
  creates the realm roles `administrators`, `behandelaars`, `beheerders`,
  `coordinators`, `raadplegers` and `recordmanagers` and attaches each one to
  the group of the same name.
- New `pabc.seedJob.keycloak.enabled` (default `true`, only active together
  with `pabc.seedJob.enabled`) runs `pabc-keycloak-groups-job-<checksum>`.
  Through the Keycloak admin API it creates, per functional role in
  `files/pabc-dataset.json`, a realm role and a group with that name, plus the
  `podiumd-admin` realm role and the `zac`/`pabc` client roles, and attaches
  them to the group. Additive only, so also safe with `skipRoles`/`skipGroups:
  true`. Needs `keycloak-operator.jobs.ensureOperatorSa.clientSecret`.
- No new image: the Job reuses `keycloak.image`.
- Users still have to be put in the groups. PABC only grants rights for the
  entity types (zaaktypen) linked to a domain in PABC.
- See [enabling-pabc.md](../apps/pabc/enabling-pabc.md) and
  [pabc-iam-migration.md](../apps/pabc/pabc-iam-migration.md).
