apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiss.fullname" . }}
  labels:
    {{- include "kiss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiss.selectorLabels" . | nindent 4 }}
