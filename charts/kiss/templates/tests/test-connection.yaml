apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kiss.fullname" . }}-test-connection"
  labels:
    {{- include "kiss.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kiss.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
