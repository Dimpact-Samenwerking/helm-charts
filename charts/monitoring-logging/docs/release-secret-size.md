# monitoring-logging — release Secret size tracking

Estimated size of the base64(gzip(json)) payload Helm stores in the
`sh.helm.release.v1.*` Secret for this chart, versus the Kubernetes
1 MiB (1,048,576 byte) Secret/ConfigMap hard limit. Generated by
`scripts/helm-release-secret-size.py`; one row per released version.

| Version | Encoded bytes | % of 1 MiB limit | Date |
|---|---|---|---|
| 1.0.15 | 406,992 | 38.8% | 2026-08-10 |
