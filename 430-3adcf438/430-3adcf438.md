# helm-charts #430: chore(deps): update maykinmedia/referentielijsten-api docker tag to v0.7.4

- URL: https://github.com/Dimpact-Samenwerking/helm-charts/pull/430
- Author: app/renovate
- Head SHA: 3adcf43815a5075c62a18d9b431ae9c56f39b8d2
- PR updated: 2026-09-18T12:11:36Z
- Reviewed at: 2026-09-22T20:05:23.284834+00:00

---

No self-contained defects are visible in the one-line image tag/digest bump.

One assumed maintenance risk: the release notes state that `UWSGI_PORT` was renamed to `REFERENTIELIJSTEN_PORT` in the new image. If this chart or any consuming values file still sets `UWSGI_PORT`, that override may be ignored after the bump. This is not provable from the diff alone because the relevant environment configuration is not shown.

```json
[
  {
    "file": "charts/podiumd/values.yaml",
    "line": 2326,
    "tag": "ASSUMED",
    "severity": "Medium",
    "category": "correctness",
    "summary": "The 0.7.4 image renames the uWSGI port environment variable from UWSGI_PORT to REFERENTIELIJSTEN_PORT, so any existing UWSGI_PORT override in this chart or consuming values may be ignored.",
    "failure_scenario": "A deployment that sets UWSGI_PORT to a custom port will start the new image with the default port, causing the service to listen on an unexpected port or fail health checks."
  }
]
```
